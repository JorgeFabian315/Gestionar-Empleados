<UserControl x:Class="Nomina.Views.IndexCategoriasView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:views="clr-namespace:Nomina.Views.CateoriasView"
             xmlns:local="clr-namespace:Nomina.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <views:CategoriasView x:Key="categorias"/>
    </UserControl.Resources>
    <Grid>
        <ContentControl>
            <ContentControl.Resources>
                <Style TargetType="ContentControl">
                    <Setter Property="Content"
                            Value="{StaticResource categorias}" />
                </Style>
            </ContentControl.Resources>
        </ContentControl>
    </Grid>
</UserControl>
